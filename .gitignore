node_modules
.next
out
.DS_Store
*.log
.env
.env*.local
.vercel
scripts/screens/
scripts/playwright-out/

# Build artefacts
tsconfig.tsbuildinfo

# Source video (100MB+) — only the compressed MP4 in public/ ships
*.mov
# Raw WhatsApp source clips in repo root — only the copy in public/ ships
/WhatsApp Video*.mp4

# Sensitive partner data (contact emails, fallback pricing)
StatDoctor Hospitals*.csv

# One-off references not part of the running site
public/dna-reference.png
